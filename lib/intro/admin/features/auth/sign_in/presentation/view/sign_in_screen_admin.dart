import 'package:flutter/material.dart';

class SignInScreenAdmin extends StatelessWidget {
  const SignInScreenAdmin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
